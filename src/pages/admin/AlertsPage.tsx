
import React from 'react';
import AdminDashboard from '../AdminDashboard';

const AlertsPage: React.FC = () => {
  return (
    <AdminDashboard initialSection="alerts" />
  );
};

export default AlertsPage;
