
import React from 'react';
import AdminDashboard from '../AdminDashboard';

const InventoryPage: React.FC = () => {
  return (
    <AdminDashboard initialSection="inventory" />
  );
};

export default InventoryPage;
