
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:development": "node deploy.js development",
    "build:staging": "node deploy.js staging",
    "build:production": "node deploy.js production",
    "serve": "vite preview",
    "test": "vitest run",
    "debug:build": "vite build --debug"
  }
}
